# Optional: the PWA can also collect this from the encrypted in-app connection form.
WEREAD_API_KEY=
# Required in production to encrypt device API keys.
SESSION_SECRET=generate-a-long-random-secret
# Optional: when set, visitors must unlock the app before entering their own key.
APP_ACCESS_TOKEN=
