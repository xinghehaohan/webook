# Optional: the PWA can also collect this from the encrypted in-app connection form.
WEREAD_API_KEY=
# Optional: only needed together with APP_ACCESS_TOKEN.
SESSION_SECRET=
# Optional: when set, visitors must unlock the app before entering a key.
APP_ACCESS_TOKEN=
